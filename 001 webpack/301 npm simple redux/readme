Сделать импорт и экспорт простых функций

    -функция запись в стор
    -функция чтение с стора

    -простое подключение компонента на прослушку нужных изменение

    -функция чтение стора


    все данные храненят предыдущее значения
    
    (тип данных form/input/ajax/simple)
    form и input - забрать из (react-redux-form) valid/submit/touched
    ajax
        -тип данных хранения (isFetching: false, isLoaded: true,)