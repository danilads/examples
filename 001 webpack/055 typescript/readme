
0) [node -v] - v22.11.0

1) [install] - yarn install (use yarn, not npm)

2) [how to run dev-mode] - yarn live

3) [how to build] - yarn build

4) [how run dist (builded project) - need to use lite-server]
    [run - yarn build]
    [add this to ./dist "package.json" and run - yarn install / yarn live]
    
    {
        "name": "",
        "version": "1.0.0",
        "description": "",
        "main": "index.html",
        "scripts": {
            "live": "lite-server"
        },
        "author": "",
        "license": "ISC",
        "devDependencies": {
            "lite-server": "^2.6.1"
        }
    }