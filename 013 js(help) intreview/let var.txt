var - видна везде в функции (объявление будет сверху ФУНКЦИИ(не блока) без присваивания, только значение будет undefined)
let - доступна внутри БЛОКА{...}(не функции), в котором объявлена.
function - будет видна сверху ФУНКЦИИ(не блока)