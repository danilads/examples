// По умолчанию sort сортирует как строки 
// Сортировка строк - 1, 11, 12, 13, 4, 9
// Сортировка чисел - 1, 4, 9, 11, 12, 13

// Сортировака массива строк - .sort()
// Сортировака массива чисел - .sort((a, b) => a > b)


// Сортировка массива объектов по ключу для строк - .sort((a, b) => a.it.localeCompare(b.it))
// Сортировка массива объектов по ключу для чисел - .sort((a, b) => a.it > b.it)

